{
  "name": "comote",
  "version": "0.8.1",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Gu-ZT/Comote.git"
  },
  "homepage": "https://github.com/Gu-ZT/Comote#readme",
  "bugs": {
    "url": "https://github.com/Gu-ZT/Comote/issues"
  },
  "private": false,
  "type": "module",
  "description": "GugleComote local remote companion for Codex Desktop.",
  "bin": {
    "comote": "bin/comote.js"
  },
  "files": [
    "src",
    "public",
    "bin"
  ],
  "scripts": {
    "app:build": "node scripts/build-macos-app.mjs",
    "desktop:icons": "node scripts/generate-desktop-assets.mjs",
    "desktop:dev": "npm run desktop:icons && node scripts/prepare-desktop-dev.mjs && npm run sidecar:build && tauri dev --config src-tauri/tauri.dev.conf.json",
    "dist:mac": "npm run desktop:icons && npm run sidecar:build:mac && node scripts/install-prod-deps.mjs && tauri build --bundles app --target aarch64-apple-darwin && node scripts/sign-macos-app.mjs && node scripts/create-mac-dmg.mjs",
    "dist:win": "npm run desktop:icons && npm run sidecar:build:win && node scripts/install-prod-deps.mjs && tauri build --bundles nsis --target x86_64-pc-windows-msvc && node scripts/collect-tauri-artifacts.mjs win",
    "sidecar:build": "node scripts/build-sidecar.mjs current",
    "sidecar:build:mac": "node scripts/build-sidecar.mjs aarch64-apple-darwin",
    "sidecar:build:win": "node scripts/build-sidecar.mjs x86_64-pc-windows-msvc",
    "dev": "node src/server/index.js",
    "wechat:check": "node scripts/check-wechat-channel.mjs",
    "test": "node --test"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.1"
  },
  "dependencies": {
    "@larksuiteoapi/node-sdk": "^1.56.1",
    "dingtalk-stream": "^2.1.6-beta.1",
    "qrcode-generator": "^1.4.4"
  },
  "overrides": {
    "axios": "1.16.1"
  }
}
